# Telegraph Finder 生产环境配置示例
# 复制此文件为 .env 并修改相应值

# 运行环境
NODE_ENV=production

# 服务器端口
PORT=3000

# 会话密钥（请使用强随机字符串）
SESSION_SECRET=change-this-to-a-random-secret-key

# 管理员密码（可选，如不设置将使用随机密码）
ADMIN_PASSWORD=your-secure-admin-password

# 数据库路径（可选，默认为 ./auth.db）
DB_PATH=./auth.db

# 上传文件存储路径（可选，默认为 ./uploads）
UPLOAD_PATH=./uploads

# 日志级别（可选：error, warn, info, debug）
LOG_LEVEL=info

# 是否启用详细日志（可选：true/false）
VERBOSE_LOGGING=false
